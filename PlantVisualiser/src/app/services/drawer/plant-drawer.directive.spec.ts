import { PlantDrawerDirective } from './plant-drawer.directive';

describe('PlantDrawerDirective', () => {
  it('should create an instance', () => {
    const directive = new PlantDrawerDirective();
    expect(directive).toBeTruthy();
  });
});
